{
  "name": "@biashara-mall/tests",
  "version": "0.0.0",
  "private": true,
  "nx": {
    "targets": {
      "test": {
        "executor": "nx:run-commands",
        "cache": true,
        "inputs": ["default", "^default"],
        "options": {
          "command": "bun test",
          "cwd": "tests"
        }
      }
    }
  }
}
